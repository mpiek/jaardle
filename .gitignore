# Backups & intermediate artifacts
*.bak*
*.pre-cleanup

# Vertaal-workers (lokale PID-bestanden)
.vertaal_*.pid

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
.mcp.json

# Firebase wordt via CDN geladen — geen npm-install in deze repo.
node_modules/
package.json
package-lock.json

# COLRv1-bronfont voor de emoji-subset (build-emoji-font.py); niet committen — te groot
fonts/Noto-COLRv1.ttf
